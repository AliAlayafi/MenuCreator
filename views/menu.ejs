<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu Editor - MenuCreator</title>
    <link href="https://cdn.jsdelivr.net/npm/daisyui@5" rel="stylesheet" type="text/css" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="min-h-screen bg-gray-50 font-inter">
    <%- include('components/loading') %>
    
    <input class="message" type="text" value="t" style="display: none;">

    <!-- Header -->
    <header class="bg-white border-b border-gray-200 sticky top-0 z-40">
        <div class="w-full lg:max-w-7xl lg:mx-auto px-2 sm:px-4 md:px-6 lg:px-8">
            <div class="flex justify-between items-center h-14 sm:h-16">
                <!-- Logo & Back -->
                <div class="flex items-center space-x-2 sm:space-x-4">
                    <a href="/main" class="flex items-center space-x-2 sm:space-x-3 hover:opacity-80 transition-opacity">
                        <div class="w-7 h-7 sm:w-8 sm:h-8 bg-purple-600 rounded-lg flex items-center justify-center">
                            <i class="fas fa-arrow-left text-white text-xs sm:text-sm"></i>
                        </div>
                        <div class="hidden sm:block">
                            <h1 class="text-base sm:text-lg font-bold text-gray-900">Menu Editor</h1>
                            <p class="text-xs text-gray-500">Create your digital menu</p>
                        </div>
                        <div class="sm:hidden">
                            <h1 class="text-sm font-bold text-gray-900">Menu Editor</h1>
                        </div>
                    </a>
                </div>
                
                <!-- Actions -->
                <div class="flex items-center space-x-1 sm:space-x-3">
                    <button id="ai-camera" class="flex items-center space-x-1 sm:space-x-2 px-2 sm:px-4 py-1.5 sm:py-2 bg-gradient-to-r from-purple-500 to-indigo-600 text-white rounded-lg hover:from-purple-600 hover:to-indigo-700 transition-colors font-medium cursor-pointer text-xs sm:text-sm shadow-lg">
                        <i class="fas fa-camera text-xs sm:text-sm"></i>
                        <span class="hidden sm:inline">Smart Scan</span>
                    </button>
                    <button id="save" class="flex items-center space-x-1 sm:space-x-2 px-2 sm:px-4 py-1.5 sm:py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 transition-colors font-medium cursor-pointer text-xs sm:text-sm">
                        <i class="fas fa-save text-xs sm:text-sm"></i>
                        <span>Save</span>
                    </button>
                    <button id="add-section" class="flex items-center space-x-1 sm:space-x-2 px-2 sm:px-4 py-1.5 sm:py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors font-medium cursor-pointer text-xs sm:text-sm">
                        <i class="fas fa-plus text-xs sm:text-sm"></i>
                        <span>Add Section</span>
                    </button>
                </div>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="w-full lg:max-w-6xl lg:mx-auto px-2 sm:px-4 md:px-6 lg:px-8 py-4 sm:py-6 md:py-8">
        <!-- Welcome Section -->
        <div class="mb-4 sm:mb-6 md:mb-8">
            <div class="bg-gradient-to-r from-purple-600 to-purple-700 rounded-xl sm:rounded-2xl p-4 sm:p-6 md:p-8 text-white">
                <div class="flex items-center space-x-3 sm:space-x-4">
                    <div class="w-12 h-12 sm:w-14 sm:h-14 md:w-16 md:h-16 bg-white/20 rounded-xl sm:rounded-2xl flex items-center justify-center">
                        <i class="fas fa-utensils text-white text-lg sm:text-xl md:text-2xl"></i>
                    </div>
                    <div>
                        <h1 class="text-lg sm:text-xl md:text-2xl font-bold mb-1 sm:mb-2">Menu Editor</h1>
                        <p class="text-purple-100 text-sm sm:text-base">Create sections, add items, and arrange them with <span class="bg-white/20 px-2 py-1 rounded-lg font-semibold text-white">drag-and-drop</span></p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Editor Container -->
        <div class="bg-white rounded-lg sm:rounded-xl shadow-sm border border-gray-200">
            <div class="px-3 sm:px-4 md:px-6 py-3 sm:py-4 border-b border-gray-100">
                <div class="flex items-center justify-between">
                    <h2 class="text-base sm:text-lg font-semibold text-gray-900">Menu Sections</h2>
                    <div class="hidden sm:flex items-center space-x-2 text-xs text-gray-500">
                        <i class="fas fa-grip-vertical"></i>
                        <span>Drag to reorder</span>
                    </div>
                </div>
            </div>
            
            <!-- Menu Container -->
            <div class="p-3 sm:p-4 md:p-6">
                <div id="menu-container" class="space-y-3">
                    <!-- Sections will be dynamically added here -->
                </div>
                
                <!-- Empty State -->
                <div id="empty-state" class="text-center py-6 sm:py-8">
                    <div class="w-12 h-12 sm:w-16 sm:h-16 bg-gray-100 rounded-lg sm:rounded-xl flex items-center justify-center mx-auto mb-3 sm:mb-4">
                        <i class="fas fa-plus text-gray-400 text-lg sm:text-2xl"></i>
                    </div>
                    <h3 class="text-sm sm:text-base font-medium text-gray-900 mb-1 sm:mb-2">No sections yet</h3>
                    <p class="text-xs sm:text-sm text-gray-500 mb-3 sm:mb-4">Add your first section to start building your menu</p>
                    <button id="add-first-section" class="px-3 sm:px-4 py-1.5 sm:py-2 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors font-medium cursor-pointer text-xs sm:text-sm">
                        <i class="fas fa-plus mr-1 sm:mr-1.5"></i>
                        Add First Section
                    </button>
                </div>
            </div>
        </div>
    </main>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/Sortable/1.15.2/Sortable.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="/js/alert.js"></script> 
    <script src="/js/menu.js"></script>
    <script src="/js/save-menu.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</body>
</html>

<script>
    let data = <%- JSON.stringify(data) %>;
</script>


