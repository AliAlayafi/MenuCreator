<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/daisyui@5" rel="stylesheet" type="text/css" />

</head>
<body class="min-h-screen flex justify-center items-center bg-gradient-to-r from-gray-900 via-gray-800 to-gray-700 relative">

    <input class="message" type="text" value="<%= alert %>" style="display: none;">


    <!-- Floating Abstract Shapes -->
    <div class="absolute w-72 h-72 bg-purple-500 opacity-30 blur-3xl rounded-full top-10 left-10"></div>
    <div class="absolute w-96 h-96 bg-indigo-500 opacity-30 blur-3xl rounded-full bottom-10 right-10"></div>

    <div class="flex items-center justify-center shadow-lg relative z-10">
        <div class="w-full max-w-lg bg-white/10 backdrop-blur-lg shadow-xl rounded-lg overflow-hidden flex flex-col md:flex-row border border-white/20">
            
            <!-- Login Form -->
            <form action="/login" method="post" class="w-full md:w-1/2 p-6 flex flex-col justify-center">
                <h2 class="text-2xl font-bold text-white text-center mb-6">Welcome Back</h2>
                
                <input type="text" name="username" placeholder="Username" 
                       class="w-full border px-3 py-2 rounded-md bg-gray-200 text-gray-900" required />
                
                <input type="password" name="password" placeholder="Password" 
                       class="w-full border border-gray-300 px-3 py-2 rounded-md mt-3 bg-gray-200 text-gray-900" required />
                
                <button class="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 rounded-md mt-4 transition cursor-pointer">
                    Login
                </button>
                
                <a href="/register" class="text-sm text-blue-300 mt-3 text-center block hover:underline">
                    Create an account
                </a>
            </form>
    
            <!-- Steps Section (Right Side) -->
            <div class="w-full md:w-1/2 bg-gradient-to-b from-purple-600 to-indigo-600 text-white flex flex-col justify-center items-center p-6 relative">
                <div class="absolute top-0 left-0 w-full h-full bg-white/10 backdrop-blur-lg rounded-lg"></div>
                
                <h3 class="text-lg font-semibold mb-4 relative z-10">Get Started</h3>
                
                <div class="space-y-4 relative z-10">
                    <div class="flex items-center gap-2">
                        <span class="text-xl">✅</span> <span>Login</span>
                    </div>
                    <div class="flex items-center gap-2">
                        <span class="text-xl">📌</span> <span>Create Your Menu</span>
                    </div>
                    <div class="flex items-center gap-2">
                        <span class="text-xl">🎉</span> <span>Get Your QR Code</span>
                    </div>
                </div>
            </div>
    
        </div>
    </div>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/js/alert.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</body>
</html>

<!-- Notify -->
<script>
    const message = document.querySelector(".message").value;
    if(message){
        alertError(message)
    }
</script>
