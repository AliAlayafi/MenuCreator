<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link href="https://cdn.jsdelivr.net/npm/daisyui@5" rel="stylesheet" type="text/css" />

</head>
<body class="min-h-screen flex justify-center items-center bg-gradient-to-r from-gray-900 via-gray-800 to-gray-700 relative">
    
    <input class="message" type="text" value="<%= alert %>" style="display: none;">

    <!-- Floating Abstract Shapes -->
    <div class="absolute w-72 h-72 bg-purple-500 opacity-30 blur-3xl rounded-full top-10 left-10"></div>
    <div class="absolute w-96 h-96 bg-indigo-500 opacity-30 blur-3xl rounded-full bottom-10 right-10"></div>

    <form action="/register" method="post" 
          class="w-full max-w-md bg-white/10 backdrop-blur-lg shadow-xl rounded-lg p-8 flex flex-col gap-4 items-center border border-white/20 relative z-10">

        <h2 class="text-2xl font-semibold text-white text-center">Create Account</h2>

        <input type="text" name="username" placeholder="Username"
               class="w-full border border-gray-300 px-3 py-2 rounded-md bg-gray-200 text-gray-900" required />

        <input type="email" name="email" placeholder="Email" 
               class="w-full border border-gray-300 px-3 py-2 rounded-md bg-gray-200 text-gray-900" required />

        <input type="password" name="password" placeholder="Password" 
               class="w-full border border-gray-300 px-3 py-2 rounded-md bg-gray-200 text-gray-900" required />

        <button class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 rounded-md transition cursor-pointer">
            Register
        </button>

        <a href="/login" class="text-sm text-blue-300 hover:underline">Already have an account?</a>

    </form>
    
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/js/alert.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</body>
</html>

<!-- Notify -->
<script>
    const message = document.querySelector(".message").value;
    if(message){
        alertError(message)
    }
</script>